services:
  # Frontend Service
  - type: web
    name: pinit-frontend
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /api/*
        destination: https://pinit-image-detection.onrender.com/api/*
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_BACKEND_URL
        value: https://pinit-image-detection.onrender.com
