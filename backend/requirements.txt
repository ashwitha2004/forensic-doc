# Core API
fastapi>=0.115.0
uvicorn>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic[email]>=2.5.0
email-validator>=2.0.0
httpx>=0.27.0

# Auth
python-jose>=3.3.0
passlib>=1.7.0
bcrypt>=4.0.0
webauthn>=2.1.0

# Database
supabase>=2.4.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.18.0

# Storage / email
cloudinary>=1.40.0
emails>=0.6

# Image processing (always required)
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.11.0

# Classic ML (forensic scoring)
scikit-learn>=1.3.0

# EXIF / metadata
exifread>=3.0.0
piexif>=1.1.3

# OCR & PDF rendering (forensics pipeline)
pytesseract>=0.3.10
pdfplumber>=0.10.0
pypdfium2>=4.0.0

# ── Deep Learning (optional but strongly recommended) ──────────────────────
# CPU build of PyTorch — comment out and install the CUDA wheel instead
# if you have a GPU: https://pytorch.org/get-started/locally/
torch>=2.1.0
torchvision>=0.16.0

# ONNX Runtime — CPU/GPU inference without torch at runtime
onnxruntime>=1.17.0
# Use onnxruntime-gpu instead for NVIDIA GPU deployments:
# onnxruntime-gpu>=1.17.0

# ONNX export support
onnx>=1.15.0
